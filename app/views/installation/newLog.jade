extends ../layouts/defualt

block title
	title 新建装机日志 - #{pkg.name}

block content
	.row
		.col-md-6.col-md-offset-3
			.well.well-sm
				form#form.form-horizontal(action='/machine/installation/log/' + _id, method='post')
					fieldset
						legend.text-center 新建装机日志
						input.hidden(name='_csrf' value=csrf_token)
						input.hidden(name='_method' value='put')
						.form-group
							label.col-md-2.control-label(for='machineNo') 主机序列号
							.col-md-9
								input#machineNo.form-control(name='machineNo', type='text', placeholder='主机序列号')
						.form-group
							.col-md-12.text-right.btn-toolbar
								button#cancel.btn.btn-default(type='button') 取消
								button#save.btn.btn-primary.btn-lg(type='submit') 新建

block foot-script
	script.
		require(['jquery', 'selectize'], function ($) {
			$(function () {
				$('#cancel').click(function () {
					if (history.length) {
						history.back();
					} else {
						window.close();
					}
				});
			});
		});

